# SQLX

## Installation

Utilizamos instaladores pré-compilados para Linux, macOS e Windows.

### Linux & macOS

Open your terminal and run:

```bash
curl --proto '=https' --tlsv1.2 -LsSf https://github.com/ThiagoDSMarcelino/sqlx/releases/latest/download/sqlx-installer.sh | sh
```

### Windows

Open your terminal and run:

```bash
powershell -c "irm https://github.com/ThiagoDSMarcelino/sqlx/releases/latest/download/sqlx-installer.ps1 | iex"
```
